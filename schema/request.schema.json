{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "FeedbackRequest",
  "description": "Request body for the /feedback endpoint",
  "type": "object",
  "required": ["sentence", "target_language", "native_language"],
  "additionalProperties": false,
  "properties": {
    "sentence": {
      "type": "string",
      "minLength": 1,
      "description": "The learner's sentence in the target language to analyze"
    },
    "target_language": {
      "type": "string",
      "minLength": 2,
      "description": "The language the learner is studying (e.g., 'Spanish', 'Japanese', 'French')"
    },
    "native_language": {
      "type": "string",
      "minLength": 2,
      "description": "The learner's native language -- explanations will be written in this language (e.g., 'English')"
    }
  }
}
